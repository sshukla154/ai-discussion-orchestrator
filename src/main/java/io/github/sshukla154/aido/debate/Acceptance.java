package io.github.sshukla154.aido.debate;

/** Whether a participant would accept the other position, and thus whether the debate can end. */
public enum Acceptance {
    YES,
    NO,
    CONDITIONALLY
}
